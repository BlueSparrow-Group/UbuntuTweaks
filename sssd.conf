[sssd]
domains = aad.yourdomain.com
config_file_version = 2
services = nss, pam

[domain/aad.yourdomain.com]
id_provider = ad
access_provider = ad
override_homedir = /home/%u
default_shell = /bin/bash
use_fully_qualified_names = False
